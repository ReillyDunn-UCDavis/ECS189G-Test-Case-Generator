Use the `run.sh` file to train the model and run the inference script. Then check the outputs folder that gets created

I dunno if this will work on a Mac, it's just kind of a quick and dirty thing I haven't really tested much
